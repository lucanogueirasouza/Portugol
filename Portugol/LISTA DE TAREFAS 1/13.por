programa {
  funcao inicio() {
    real b, h, a

    escreva("Qual o valor da altura? ")
    leia(h)
    escreva("Qual o valor da base? ")
    leia(b)
    a = (b * h) / 2
    escreva("O valor da área do triangulo, é: ", a)
  }
}