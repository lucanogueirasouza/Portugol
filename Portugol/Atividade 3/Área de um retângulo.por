programa {
  funcao inicio() {
    // area de um retangulo
    real l, h, a 
    escreva("Digite a altura do retangulo: ")
    leia(h)
    escreva("Digite a largura do retangulo: ")
    leia(l)

    a = l * h

    escreva(a)
  }
}